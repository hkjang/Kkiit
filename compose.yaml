services:
  kkiit:
    image: kkiit:v0.1.1
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
