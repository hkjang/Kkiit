import { FormEvent, useEffect, useState } from 'react'
import { NavLink, Route, Routes } from 'react-router-dom'
import { Alert, Box, Button, Card, Chip, Container, Dialog, DialogActions, DialogContent, DialogTitle, Divider, IconButton, List, ListItemButton, ListItemIcon, ListItemText, MenuItem, Stack, TextField, Typography } from '@mui/material'
import PersonOutlineRoundedIcon from '@mui/icons-material/PersonOutlineRounded'
import KeyRoundedIcon from '@mui/icons-material/KeyRounded'
import NotificationsNoneRoundedIcon from '@mui/icons-material/NotificationsNoneRounded'
import StorefrontOutlinedIcon from '@mui/icons-material/StorefrontOutlined'
import DeleteOutlineRoundedIcon from '@mui/icons-material/DeleteOutlineRounded'
import AutorenewRoundedIcon from '@mui/icons-material/AutorenewRounded'
import AddRoundedIcon from '@mui/icons-material/AddRounded'
import AutoAwesomeRoundedIcon from '@mui/icons-material/AutoAwesomeRounded'
import RequestQuoteOutlinedIcon from '@mui/icons-material/RequestQuoteOutlined'
import ContentCopyRoundedIcon from '@mui/icons-material/ContentCopyRounded'
import SecurityRoundedIcon from '@mui/icons-material/SecurityRounded'
import { api, dateTime } from '../api'
import { useApp } from '../App'
import type { ApiKey } from '../types'

export function ProfilePage() {
  return <Container maxWidth="lg" sx={{ py: { xs: 3, md: 6 } }}><Stack direction={{ xs: 'column', md: 'row' }} gap={3} alignItems="flex-start">
    <Card sx={{ width: { xs: '100%', md: 250 }, p: 1.2, position: { md: 'sticky' }, top: 96 }}><List disablePadding>
      <ProfileLink to="/profile" icon={<PersonOutlineRoundedIcon />} label="내 프로필" end />
      <ProfileLink to="/profile/keys" icon={<KeyRoundedIcon />} label="API 키" />
      <ProfileLink to="/profile/security" icon={<SecurityRoundedIcon />} label="로그인 보안" />
      <ProfileLink to="/profile/seller" icon={<StorefrontOutlinedIcon />} label="판매자·상품" />
      <ProfileLink to="/profile/projects" icon={<RequestQuoteOutlinedIcon />} label="견적 프로젝트" />
      <ProfileLink to="/profile/notifications" icon={<NotificationsNoneRoundedIcon />} label="알림 설정" />
    </List></Card>
    <Box sx={{ minWidth: 0, flex: 1 }}><Routes><Route index element={<ProfileDetails />} /><Route path="keys" element={<ApiKeys />} /><Route path="security" element={<LoginSecurity />} /><Route path="seller" element={<SellerProfile />} /><Route path="projects" element={<Projects />} /><Route path="notifications" element={<Placeholder title="알림 설정" body="채널별 수신 설정은 관리자가 활성화한 채널에 맞춰 제공됩니다." />} /></Routes></Box>
  </Stack></Container>
}
function ProfileLink({ to, icon, label, end = false }: { to: string; icon: React.ReactNode; label: string; end?: boolean }) { return <ListItemButton component={NavLink} to={to} end={end} sx={{ borderRadius: 2, '&.active': { bgcolor: 'primary.light', color: 'primary.dark' } }}><ListItemIcon sx={{ minWidth: 40, color: 'inherit' }}>{icon}</ListItemIcon><ListItemText primary={label} slotProps={{ primary: { fontWeight: 700 } }} /></ListItemButton> }
function ProfileDetails() { const { me, version } = useApp(); return <><Typography variant="h2">개인화 페이지</Typography><Typography color="text.secondary" sx={{ mt: .5, mb: 3 }}>계정, 판매자·구매자 경험과 개인 키를 관리합니다.</Typography><Card sx={{ p: 3 }}><Typography variant="h3">{me?.display_name}</Typography><Typography color="text.secondary">{me?.email ?? `@${me?.username}`}</Typography><Divider sx={{ my: 3 }} /><Stack direction="row" flexWrap="wrap" gap={1}>{me?.roles.map((role) => <Chip key={role} label={role} />)}</Stack><Alert severity="info" sx={{ mt: 3 }}>현재 서비스 버전은 Kkiit v{version?.version ?? '—'}이며 MCP {version?.mcp_protocol_version ?? '—'}를 제공합니다.</Alert></Card></> }
function Placeholder({ title, body }: { title: string; body: string }) { return <><Typography variant="h2">{title}</Typography><Card sx={{ p: 4, mt: 3 }}><Typography>{body}</Typography></Card></> }

function LoginSecurity(){const {notify}=useApp();const [enabled,setEnabled]=useState(false);const [secret,setSecret]=useState('');const [uri,setUri]=useState('');const [code,setCode]=useState('');const load=()=>api<{totp_enabled:boolean}>('/api/v1/me/mfa').then(d=>setEnabled(d.totp_enabled)).catch(e=>notify(e.message,'error'));useEffect(()=>{load()},[]);const setup=async()=>{try{const result=await api<{secret:string;otpauth_uri:string}>('/api/v1/me/mfa/totp/setup',{method:'POST'});setSecret(result.secret);setUri(result.otpauth_uri);setCode('')}catch(e){notify(e instanceof Error?e.message:'설정을 시작하지 못했습니다.','error')}};const confirm=async()=>{try{await api('/api/v1/me/mfa/totp/confirm',{method:'POST',body:JSON.stringify({code})});setSecret('');setUri('');setCode('');load();notify('TOTP MFA를 활성화했습니다.','success')}catch(e){notify(e instanceof Error?e.message:'코드를 확인하지 못했습니다.','error')}};const disable=async()=>{const value=window.prompt('MFA를 해제하려면 현재 6자리 코드를 입력하세요.')??'';if(!value)return;try{await api('/api/v1/me/mfa/totp',{method:'DELETE',body:JSON.stringify({code:value})});load();notify('TOTP MFA를 해제했습니다.','success')}catch(e){notify(e instanceof Error?e.message:'MFA를 해제하지 못했습니다.','error')}};return <><Typography variant="h2">로그인 보안</Typography><Typography color="text.secondary" sx={{mt:.5,mb:3}}>관리자와 판매자 계정에 Authenticator 기반 2단계 인증을 적용할 수 있습니다.</Typography><Card sx={{p:3}}><Stack direction={{xs:'column',sm:'row'}} justifyContent="space-between" gap={2}><Box><Stack direction="row" gap={1}><Typography variant="h3">TOTP 인증</Typography><Chip label={enabled?'활성':'비활성'} color={enabled?'success':'default'} size="small"/></Stack><Typography color="text.secondary" sx={{mt:1}}>표준 Authenticator 앱과 호환되며 비밀값은 암호화해 저장합니다.</Typography></Box>{enabled?<Button color="error" onClick={disable}>MFA 해제</Button>:!secret&&<Button variant="contained" onClick={setup}>MFA 설정</Button>}</Stack>{secret&&<Box sx={{mt:3}}><Alert severity="warning">아래 비밀값을 인증 앱에 등록한 뒤 6자리 코드로 확인하세요. 비밀값은 이 단계에서만 표시됩니다.</Alert><TextField fullWidth label="TOTP 비밀값" value={secret} slotProps={{input:{readOnly:true,endAdornment:<IconButton aria-label="비밀값 복사" onClick={()=>navigator.clipboard.writeText(secret)}><ContentCopyRoundedIcon/></IconButton>}}} sx={{mt:2}}/><Typography component="code" sx={{display:'block',mt:1,fontSize:'.8rem',wordBreak:'break-all',color:'text.secondary'}}>{uri}</Typography><Stack direction={{xs:'column',sm:'row'}} gap={1.5} sx={{mt:2}}><TextField label="6자리 확인 코드" value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,'').slice(0,6))} inputProps={{inputMode:'numeric',maxLength:6}}/><Button variant="contained" onClick={confirm} disabled={code.length!==6}>활성화 확인</Button></Stack></Box>}</Card></>}

type MyTalent = { id:string;title:string;status:string;service_type:string;base_price:number;currency:string;delivery_days:number;updated_at:string }
function SellerProfile(){
  const {me,refreshMe,notify}=useApp();const [enabled,setEnabled]=useState(me?.roles.includes('seller')??false);const [headline,setHeadline]=useState('');const [skills,setSkills]=useState('');const [talents,setTalents]=useState<MyTalent[]>([]);const [open,setOpen]=useState(false);const [aiIdea,setAiIdea]=useState('');const [aiBusy,setAiBusy]=useState(false);const [form,setForm]=useState({title:'',summary:'',description:'',base_price:'100000',delivery_days:'3',service_type:'HUMAN'})
  const load=()=>{api<{enabled:boolean;headline?:string;skills?:string[]}>('/api/v1/me/seller-profile').then(data=>{setEnabled(data.enabled);setHeadline(data.headline??'');setSkills((data.skills??[]).join(', '))});if(me?.permissions.includes('talents.write'))api<{items:MyTalent[]}>('/api/v1/me/talents').then(data=>setTalents(data.items))}
  useEffect(()=>{load()},[me?.permissions])
  const saveProfile=async(event:FormEvent)=>{event.preventDefault();try{await api('/api/v1/me/seller-profile',{method:'PUT',body:JSON.stringify({seller_type:'individual',headline,biography:'',skills:skills.split(',').map(x=>x.trim()).filter(Boolean),capacity:5,settings:{}})});await refreshMe();setEnabled(true);notify('판매자 프로필을 활성화했습니다.','success')}catch(e){notify(e instanceof Error?e.message:'저장하지 못했습니다.','error')}}
  const createTalent=async(event:FormEvent)=>{event.preventDefault();try{const price=Number(form.base_price),days=Number(form.delivery_days);await api('/api/v1/talents',{method:'POST',body:JSON.stringify({...form,base_price:price,delivery_days:days,currency:'KRW',revision_count:1,scope_included:[],scope_excluded:[],deliverables:[],tags:skills.split(',').map(x=>x.trim()).filter(Boolean),faq:[],refund_policy:'작업 시작 전 전액 환불',instant_order:true,quote_required:false,subscription_enabled:false,packages:[{package_type:'BASIC',name:'기본 패키지',description:form.summary,price,delivery_days:days,revision_count:1,features:[],deliverables:[],sort_order:0,active:true}],requirements:[{label:'작업에 필요한 요청사항',help_text:'목표와 참고 자료를 자세히 적어주세요.',field_type:'textarea',required:true,options:[],validation:{},sort_order:0}]})});setOpen(false);load();notify('상품 초안을 만들었습니다. 공개 요청을 진행하세요.','success')}catch(e){notify(e instanceof Error?e.message:'상품을 만들지 못했습니다.','error')}}
  const publish=async(id:string)=>{try{const result=await api<{status:string}>(`/api/v1/talents/${id}/publish`,{method:'POST'});load();notify(result.status==='published'?'상품을 공개했습니다.':'관리자 검토를 요청했습니다.','success')}catch(e){notify(e instanceof Error?e.message:'공개 요청에 실패했습니다.','error')}}
  const draftWithAI=async()=>{if(aiIdea.trim().length<10){notify('서비스 아이디어를 10자 이상 입력하세요.','warning');return};setAiBusy(true);try{const result=await api<{draft:Record<string,unknown>;meta:{mode:string;warning?:string}}>('/api/v1/ai/talents/draft',{method:'POST',body:JSON.stringify({idea:aiIdea,locale:'ko-KR'})});const draft=result.draft;setForm({...form,title:String(draft.title??''),summary:String(draft.summary??''),description:String(draft.description??aiIdea),base_price:String(draft.base_price??100000),delivery_days:String(draft.delivery_days??3)});notify(result.meta.warning??'AI 상품 초안을 만들었습니다.',result.meta.warning?'warning':'success')}catch(e){notify(e instanceof Error?e.message:'초안을 만들지 못했습니다.','error')}finally{setAiBusy(false)}}
  if(!enabled)return <><Typography variant="h2">판매자 시작하기</Typography><Typography color="text.secondary" sx={{mt:.5,mb:3}}>구매 계정과 분리된 판매 역할을 추가합니다.</Typography><Card component="form" onSubmit={saveProfile} sx={{p:3}}><Stack spacing={2}><TextField label="판매자 한 줄 소개" value={headline} onChange={e=>setHeadline(e.target.value)} required/><TextField label="전문 기술" value={skills} onChange={e=>setSkills(e.target.value)} helperText="쉼표로 구분하세요."/><Button type="submit" variant="contained">판매자로 전환</Button></Stack></Card></>
  return <><Stack direction="row" justifyContent="space-between" alignItems="end"><Box><Typography variant="h2">판매자·상품</Typography><Typography color="text.secondary" sx={{mt:.5}}>{headline||'전문 서비스를 상품으로 등록하세요.'}</Typography></Box><Button variant="contained" startIcon={<AddRoundedIcon/>} onClick={()=>setOpen(true)}>새 상품</Button></Stack><Stack spacing={2} sx={{mt:3}}>{talents.map(item=><Card key={item.id} sx={{p:2.5}}><Stack direction={{xs:'column',sm:'row'}} justifyContent="space-between" alignItems={{sm:'center'}} gap={2}><Box><Stack direction="row" gap={1}><Typography variant="h4">{item.title}</Typography><Chip size="small" label={item.status}/></Stack><Typography variant="body2" color="text.secondary">{item.service_type} · {item.base_price.toLocaleString()}원 · {item.delivery_days}일</Typography></Box>{['draft','rejected'].includes(item.status)&&<Button onClick={()=>publish(item.id)}>공개 요청</Button>}</Stack></Card>)}</Stack>{talents.length===0&&<Card sx={{p:5,mt:3,textAlign:'center'}}><Typography color="text.secondary">아직 등록한 상품이 없습니다.</Typography></Card>}<Dialog open={open} onClose={()=>setOpen(false)} fullWidth maxWidth="sm"><Box component="form" onSubmit={createTalent}><DialogTitle>새 재능 상품</DialogTitle><DialogContent><Stack spacing={2} sx={{mt:1}}><Card variant="outlined" sx={{p:2,boxShadow:'none',bgcolor:'#f8faff'}}><Typography fontWeight={750}>AI 빠른 초안</Typography><TextField fullWidth multiline minRows={2} value={aiIdea} onChange={e=>setAiIdea(e.target.value)} placeholder="예: Spring Boot 시스템 성능을 점검하고 보고서를 작성해요." sx={{mt:1}}/><Button startIcon={<AutoAwesomeRoundedIcon/>} onClick={draftWithAI} disabled={aiBusy} sx={{mt:1}}>{aiBusy?'초안 생성 중…':'AI로 채우기'}</Button></Card><TextField label="상품명" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} required/><TextField label="한 줄 소개" value={form.summary} onChange={e=>setForm({...form,summary:e.target.value})} required/><TextField label="상세 설명" multiline minRows={4} value={form.description} onChange={e=>setForm({...form,description:e.target.value})} required/><TextField select label="서비스 유형" value={form.service_type} onChange={e=>setForm({...form,service_type:e.target.value})}><MenuItem value="HUMAN">전문가 작업</MenuItem><MenuItem value="AI">AI 자동 실행</MenuItem><MenuItem value="HYBRID">AI + 전문가 검수</MenuItem></TextField><TextField label="기본 가격" type="number" value={form.base_price} onChange={e=>setForm({...form,base_price:e.target.value})}/><TextField label="기본 납기(일)" type="number" value={form.delivery_days} onChange={e=>setForm({...form,delivery_days:e.target.value})}/></Stack></DialogContent><DialogActions><Button onClick={()=>setOpen(false)}>취소</Button><Button type="submit" variant="contained">초안 만들기</Button></DialogActions></Box></Dialog></>
}

type RFQ={id:string;buyer_id:string;title:string;description:string;budget_min?:number;budget_max?:number;currency:string;desired_due_at?:string;state:string;created_at:string}
type Quote={id:string;seller:{display_name:string};amount:number;currency:string;delivery_days:number;match_score:number;state:string}
function Projects(){const {me,notify}=useApp();const [items,setItems]=useState<RFQ[]>([]);const [quotes,setQuotes]=useState<Quote[]>([]);const [open,setOpen]=useState(false);const [quoteFor,setQuoteFor]=useState<RFQ|null>(null);const [selected,setSelected]=useState<RFQ|null>(null);const [form,setForm]=useState({title:'',description:'',budget_max:'500000',delivery_days:'7',amount:'500000'});const load=()=>api<{items:RFQ[]}>('/api/v1/rfqs').then(data=>setItems(data.items)).catch(e=>notify(e.message,'error'));useEffect(()=>{load()},[]);const create=async(event:FormEvent)=>{event.preventDefault();try{await api('/api/v1/rfqs',{method:'POST',body:JSON.stringify({title:form.title,description:form.description,requirements:{},budget_max:Number(form.budget_max),currency:'KRW'})});setOpen(false);load();notify('견적 요청을 공개했습니다.','success')}catch(e){notify(e instanceof Error?e.message:'등록 실패','error')}};const submitQuote=async(event:FormEvent)=>{event.preventDefault();if(!quoteFor)return;try{await api('/api/v1/quotes',{method:'POST',body:JSON.stringify({rfq_id:quoteFor.id,amount:Number(form.amount),currency:'KRW',delivery_days:Number(form.delivery_days),scope:{description:'요구사항 기준 작업'},milestones:[]})});setQuoteFor(null);notify('견적을 제출했습니다.','success')}catch(e){notify(e instanceof Error?e.message:'견적 실패','error')}};const viewQuotes=async(rfq:RFQ)=>{try{setSelected(rfq);setQuotes((await api<{items:Quote[]}>(`/api/v1/rfqs/${rfq.id}/quotes`)).items)}catch(e){notify(e instanceof Error?e.message:'조회 실패','error')}};return <><PageTitleLocal title="견적 프로젝트" description="원하는 상품을 찾지 못하면 프로젝트를 등록하고 판매자 견적을 비교하세요." action={<Button variant="contained" startIcon={<AddRoundedIcon/>} onClick={()=>setOpen(true)}>프로젝트 등록</Button>}/><Stack spacing={2}>{items.map(item=><Card key={item.id} sx={{p:2.5}}><Stack direction={{xs:'column',sm:'row'}} justifyContent="space-between" gap={2}><Box><Stack direction="row" gap={1}><Typography variant="h4">{item.title}</Typography><Chip size="small" label={item.state}/></Stack><Typography variant="body2" color="text.secondary" sx={{mt:.5}}>{item.description}</Typography><Typography sx={{mt:1}}>{item.budget_max?`예산 최대 ${item.budget_max.toLocaleString()}원`:'예산 협의'}</Typography></Box>{item.buyer_id===me?.id?<Button onClick={()=>viewQuotes(item)}>견적 비교</Button>:me?.roles.includes('seller')?<Button onClick={()=>setQuoteFor(item)}>견적 제출</Button>:null}</Stack></Card>)}</Stack>{items.length===0&&<Card sx={{p:5,textAlign:'center'}}><Typography color="text.secondary">등록된 견적 프로젝트가 없습니다.</Typography></Card>}<Dialog open={open} onClose={()=>setOpen(false)} fullWidth maxWidth="sm"><Box component="form" onSubmit={create}><DialogTitle>프로젝트 견적 요청</DialogTitle><DialogContent><Stack spacing={2} sx={{mt:1}}><TextField label="프로젝트 제목" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} required/><TextField multiline minRows={5} label="상세 요구사항" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} required/><TextField type="number" label="최대 예산" value={form.budget_max} onChange={e=>setForm({...form,budget_max:e.target.value})}/></Stack></DialogContent><DialogActions><Button onClick={()=>setOpen(false)}>취소</Button><Button type="submit" variant="contained">공개</Button></DialogActions></Box></Dialog><Dialog open={Boolean(quoteFor)} onClose={()=>setQuoteFor(null)} fullWidth maxWidth="sm"><Box component="form" onSubmit={submitQuote}><DialogTitle>견적 제출 · {quoteFor?.title}</DialogTitle><DialogContent><Stack spacing={2} sx={{mt:1}}><TextField type="number" label="견적 금액" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/><TextField type="number" label="예상 작업일" value={form.delivery_days} onChange={e=>setForm({...form,delivery_days:e.target.value})}/></Stack></DialogContent><DialogActions><Button onClick={()=>setQuoteFor(null)}>취소</Button><Button type="submit" variant="contained">제출</Button></DialogActions></Box></Dialog><Dialog open={Boolean(selected)} onClose={()=>setSelected(null)} fullWidth maxWidth="md"><DialogTitle>AI 견적 비교 · {selected?.title}</DialogTitle><DialogContent><Stack spacing={1.5} sx={{mt:1}}>{quotes.map(item=><Card key={item.id} variant="outlined" sx={{p:2,boxShadow:'none'}}><Stack direction="row" justifyContent="space-between"><Box><Typography fontWeight={750}>{item.seller.display_name}</Typography><Typography variant="body2">예상 {item.delivery_days}일 · 적합도 {Number(item.match_score).toFixed(0)}점</Typography></Box><Typography variant="h4">{item.amount.toLocaleString()}원</Typography></Stack></Card>)}</Stack>{quotes.length===0&&<Alert severity="info">아직 접수된 견적이 없습니다.</Alert>}</DialogContent><DialogActions><Button onClick={()=>setSelected(null)}>닫기</Button></DialogActions></Dialog></>}
function PageTitleLocal({title,description,action}:{title:string;description:string;action?:React.ReactNode}){return <Stack direction={{xs:'column',sm:'row'}} justifyContent="space-between" alignItems={{sm:'end'}} gap={2} sx={{mb:3}}><Box><Typography variant="h2">{title}</Typography><Typography color="text.secondary" sx={{mt:.5}}>{description}</Typography></Box>{action}</Stack>}

function ApiKeys() {
  const { me, notify } = useApp(); const [keys, setKeys] = useState<ApiKey[]>([]); const [open, setOpen] = useState(false); const [name, setName] = useState('내 연동 키'); const [secret, setSecret] = useState(''); const [loading, setLoading] = useState(true)
  const load = () => api<{ items: ApiKey[] }>('/api/v1/me/api-keys').then((data) => setKeys(data.items)).finally(() => setLoading(false))
  useEffect(() => { void load() }, [])
  const create = async (event: FormEvent) => { event.preventDefault(); try { const result = await api<{ secret: string }>('/api/v1/me/api-keys', { method: 'POST', body: JSON.stringify({ name, scopes: me?.permissions.filter((item) => ['mcp.use', 'orders.buy', 'orders.sell', 'talents.write'].includes(item)), allowed_cidrs: [], rate_limit_per_minute: 60 }) }); setSecret(result.secret); setOpen(false); load() } catch (cause) { notify(cause instanceof Error ? cause.message : '키를 만들지 못했습니다.', 'error') } }
  const rotate = async (id: string) => { try { const result = await api<{ secret: string }>(`/api/v1/me/api-keys/${id}/rotate`, { method: 'POST' }); setSecret(result.secret); load() } catch (cause) { notify(cause instanceof Error ? cause.message : '키를 회전하지 못했습니다.', 'error') } }
  const revoke = async (id: string) => { if (!window.confirm('이 API 키를 즉시 폐기할까요? 되돌릴 수 없습니다.')) return; try { await api(`/api/v1/me/api-keys/${id}`, { method: 'DELETE' }); load(); notify('API 키를 폐기했습니다.', 'success') } catch (cause) { notify(cause instanceof Error ? cause.message : '키를 폐기하지 못했습니다.', 'error') } }
  return <><Stack direction="row" justifyContent="space-between" alignItems="end"><Box><Typography variant="h2">개인 API 키</Typography><Typography color="text.secondary" sx={{ mt: .5 }}>키마다 최소 권한, IP 제한, 속도와 만료를 적용할 수 있습니다.</Typography></Box><Button variant="contained" startIcon={<AddRoundedIcon />} onClick={() => setOpen(true)}>키 만들기</Button></Stack>
    {secret && <Alert severity="warning" sx={{ mt: 3 }} action={<IconButton aria-label="키 복사" onClick={() => { navigator.clipboard.writeText(secret); notify('키를 복사했습니다.', 'success') }}><ContentCopyRoundedIcon /></IconButton>}><Typography fontWeight={750}>새 키를 지금 안전한 곳에 보관하세요. 다시 표시되지 않습니다.</Typography><Box component="code" sx={{ display: 'block', mt: 1, wordBreak: 'break-all' }}>{secret}</Box></Alert>}
    <Stack spacing={2} sx={{ mt: 3 }}>{keys.map((key) => <Card key={key.id} sx={{ p: 2.5, opacity: key.revoked_at ? .58 : 1 }}><Stack direction={{ xs: 'column', sm: 'row' }} justifyContent="space-between" alignItems={{ sm: 'center' }} gap={2}><Box><Stack direction="row" spacing={1} alignItems="center"><Typography variant="h4">{key.name}</Typography>{key.revoked_at && <Chip label="폐기됨" size="small" />}</Stack><Typography variant="body2" color="text.secondary" sx={{ mt: .5 }}>kkiit_{key.prefix}_•••• · 생성 {dateTime(key.created_at)} · 최근 사용 {dateTime(key.last_used_at)}</Typography><Stack direction="row" gap={.6} flexWrap="wrap" sx={{ mt: 1 }}>{key.scopes.map((scope) => <Chip key={scope} label={scope} size="small" variant="outlined" />)}</Stack></Box>{!key.revoked_at && <Stack direction="row"><IconButton aria-label={`${key.name} 회전`} onClick={() => rotate(key.id)}><AutorenewRoundedIcon /></IconButton><IconButton aria-label={`${key.name} 폐기`} color="error" onClick={() => revoke(key.id)}><DeleteOutlineRoundedIcon /></IconButton></Stack>}</Stack></Card>)}</Stack>
    {!loading && keys.length === 0 && <Card sx={{ p: 5, mt: 3, textAlign: 'center' }}><KeyRoundedIcon color="disabled" sx={{ fontSize: 46 }} /><Typography variant="h3" sx={{ mt: 1 }}>아직 API 키가 없습니다</Typography></Card>}
    <Dialog open={open} onClose={() => setOpen(false)} fullWidth maxWidth="sm"><Box component="form" onSubmit={create}><DialogTitle>새 API 키 만들기</DialogTitle><DialogContent><TextField autoFocus fullWidth label="키 이름" value={name} onChange={(e) => setName(e.target.value)} sx={{ mt: 1 }} helperText="용도를 알아볼 수 있는 이름을 사용하세요." /></DialogContent><DialogActions><Button onClick={() => setOpen(false)}>취소</Button><Button type="submit" variant="contained">만들기</Button></DialogActions></Box></Dialog>
  </>
}
